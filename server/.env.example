# Database Configuration (Docker local)
DB_HOST=localhost
DB_USER=busstops
DB_PASSWORD=busstops123
DB_NAME=busstops

# Table prefix (use your name, e.g., danil_)
TABLE_PREFIX=danil_

# Server Configuration
PORT=3000

# GTFS Source URL (Tallinn public transport)
GTFS_URL=https://transport.tallinn.ee/data/gtfs.zip

# Timezone for arrivals calculation
TZ=Europe/Tallinn
